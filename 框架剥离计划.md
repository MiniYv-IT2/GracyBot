# GracyBot 纯框架剥离计划

> 目标：将 GracyBot 拆分为纯框架仓库，类似 NoneBot2，只提供核心能力，不含业务插件

---

## 一、目标目录结构

```
gracy_framework/
├── core/                          # 框架核心
│   ├── __init__.py                # 统一导出
│   ├── main.py                    # 入口（HTTP/WS 启动）
│   ├── plugin_manager.py          # 插件管理器
│   ├── config.py                  # 框架配置
│   ├── config_manager.py          # 配置管理器
│   ├── security.py                # 安全工具
│   ├── security_manager.py        # 安全管理器
│   ├── logger_manager.py          # 日志管理器
│   ├── utils.py                   # 通用工具
│   │
│   ├── gracy_adapter/             # 适配器层（完整保留）
│   │   ├── __init__.py
│   │   ├── adapter.py             # GracyAdapter 抽象基类
│   │   ├── event.py               # GracyEvent 事件定义
│   │   ├── message.py             # 消息段类型
│   │   ├── send.py                # 统一发送入口
│   │   ├── pool.py                # AdapterPool
│   │   ├── identity.py            # IdentityTag
│   │   ├── gracy_bot.py           # GracyBot 统一入口
│   │   ├── adapters.json          # 适配器配置模板
│   │   │
│   │   ├── onebot/                # OneBot 适配器（内置）
│   │   │   ├── __init__.py
│   │   │   ├── adapter.py
│   │   │   ├── http.py
│   │   │   ├── ws.py
│   │   │   ├── cq.py
│   │   │   ├── sanitize.py
│   │   │   ├── onebot_config.template.json  # 配置模板
│   │   │   └── onebot_config.json           # 运行时配置（.gitignore）
│   │   │
│   │   └── qq_official/           # QQ官方适配器（内置）
│   │       ├── __init__.py
│   │       ├── adapter.py
│   │       ├── api.py
│   │       ├── auth.py
│   │       ├── bind.py
│   │       ├── bot.py
│   │       ├── factory.py
│   │       ├── gateway.py
│   │       ├── media.py
│   │       ├── message.py
│   │       ├── protocol.py
│   │       └── sender.py
│   │
│   ├── pipeline/                  # 管道调度
│   │   ├── __init__.py
│   │   └── stages.py
│   │
│   ├── decorators/                # 装饰器体系
│   │   ├── __init__.py
│   │   ├── registration.py
│   │   ├── handler.py
│   │   ├── context.py
│   │   ├── security.py
│   │   ├── session.py
│   │   ├── async_utils.py
│   │   └── logger.py
│   │
│   ├── runtime/                   # 运行时上下文
│   │   ├── __init__.py
│   │   ├── runtime.py
│   │   └── data.py
│   │
│   └── event/                     # 事件总线
│       └── __init__.py
│
├── style/                         # 样式/资源
│   ├── log_colors.py              # 日志着色
│   └── styling.py                 # 日志格式化
│
├── docs/                          # 开发文档
│   ├── 01-快速入门.md
│   ├── 02-框架概述.md
│   ├── 03-安装与启动.md
│   ├── 04-项目架构.md
│   ├── 05-插件开发指南（新风格）.md
│   ├── 06-插件开发指南（旧风格）.md
│   ├── 07-新旧风格对比.md
│   ├── 08-metadata.toml规范.md
│   ├── 09-CLI命令与调试.md
│   ├── 10-消息段与适配器.md
│   ├── 11-基础API参考.md
│   ├── 12-常见问题与最佳实践.md
│   ├── 13-适配器开发指南.md
│   ├── 14-插件开发最佳实践.md
│   └── 15-日志系统.md
│
├── bot.py                         # 入口示例
├── pyproject.toml                 # 包元数据
├── requirements.txt               # 依赖清单
├── config.template.json           # 配置模板
├── README.md                      # 说明文档
├── LICENSE                        # 许可证
├── MANIFEST.in                    # 打包清单
└── .gitignore
```

---

## 二、剥离内容清单

### 必须删除

| 路径 | 说明 |
|---|---|
| `core/tools/` | CLI 命令行工具（全部） |
| `core/gracy_session/` | 会话管理模块 |
| `core/monitor.py` | 监控管理器 |
| `core/handler.py` | HTTP 回调处理（业务逻辑） |
| `core/adapter.py` | 统一适配器包装（功能已在 gracy_adapter/） |
| `plugins/` | 全部业务插件 |
| `CharacterModels/` | TTS 模型文件 |
| `_v2_models/` | AI 模型文件 |
| `.dbg/` | 调试文件 |
| `dist_extract/` | 打包产物 |
| `logs/` | 运行日志 |
| `config.json` | 运行时配置 |
| `core/gracy_adapter/onebot/onebot_config.json` | 运行时配置（保留 template） |
| `core/gracy_adapter/qq_official/logs/` | QQ官方适配器运行日志 |
| `core/__pycache__/` | 所有 `__pycache__` 目录 |
| `core/**/__pycache__/` | 所有 `__pycache__` 目录 |
| `*.bak`, `*.backup` | 备份文件 |

### 必须修改

| 文件 | 修改内容 |
|---|---|
| `core/__init__.py` | 移除 monitor_manager、callback_base 等不存在模块的导入 |
| `core/config.py` | 移除 `AUTO_REPLIES` 业务默认配置 |
| `core/utils.py` | 移除 `handle_auto_reply()` 自动回复功能 |
| `core/main.py` | 移除 monitor_manager 依赖、`_interactive_connection_setup()` 交互菜单 |
| `pyproject.toml` | 修改打包配置，只包含 core、style、docs |

---

## 三、操作步骤

### Step 1: 复制完整 core 目录

```powershell
# 在 e:\ai智能体\gracy_framework 目录下执行
xcopy "e:\ai智能体\gracybot\core" "e:\ai智能体\gracy_framework\core" /E /I /Y
```

### Step 2: 删除不需要的目录和文件

```powershell
# 删除 CLI 工具
Remove-Item "e:\ai智能体\gracy_framework\core\tools" -Recurse -Force

# 删除会话管理
Remove-Item "e:\ai智能体\gracy_framework\core\gracy_session" -Recurse -Force

# 删除监控模块
Remove-Item "e:\ai智能体\gracy_framework\core\monitor.py" -Force

# 删除 HTTP 回调处理
Remove-Item "e:\ai智能体\gracy_framework\core\handler.py" -Force

# 删除统一适配器包装
Remove-Item "e:\ai智能体\gracy_framework\core\adapter.py" -Force

# 删除运行时配置文件（保留模板）
Remove-Item "e:\ai智能体\gracy_framework\core\gracy_adapter\onebot\onebot_config.json" -Force

# 删除 QQ官方适配器运行日志
Remove-Item "e:\ai智能体\gracy_framework\core\gracy_adapter\qq_official\logs" -Recurse -Force

# 删除所有 __pycache__
Get-ChildItem -Path "e:\ai智能体\gracy_framework\core" -Recurse -Directory -Filter "__pycache__" | Remove-Item -Recurse -Force

# 删除备份文件
Get-ChildItem -Path "e:\ai智能体\gracy_framework\core" -Recurse -Include "*.bak","*.backup" | Remove-Item -Force
```

### Step 3: 复制 style 和 docs

```powershell
# 创建 style 目录
New-Item -Path "e:\ai智能体\gracy_framework\style" -ItemType Directory -Force

# 复制样式文件
Copy-Item "e:\ai智能体\gracybot\style\log_colors.py" "e:\ai智能体\gracy_framework\style\"
Copy-Item "e:\ai智能体\gracybot\style\styling.py" "e:\ai智能体\gracy_framework\style\"

# 复制文档
Copy-Item "e:\ai智能体\gracybot\docs" "e:\ai智能体\gracy_framework\docs" -Recurse -Force
```

### Step 4: 复制根目录文件

```powershell
# 复制入口文件和配置
Copy-Item "e:\ai智能体\gracybot\bot.py" "e:\ai智能体\gracy_framework\"
Copy-Item "e:\ai智能体\gracybot\pyproject.toml" "e:\ai智能体\gracy_framework\"
Copy-Item "e:\ai智能体\gracybot\requirements.txt" "e:\ai智能体\gracy_framework\"
Copy-Item "e:\ai智能体\gracybot\config.template.json" "e:\ai智能体\gracy_framework\"
Copy-Item "e:\ai智能体\gracybot\README.md" "e:\ai智能体\gracy_framework\"
Copy-Item "e:\ai智能体\gracybot\LICENSE" "e:\ai智能体\gracy_framework\"
Copy-Item "e:\ai智能体\gracybot\MANIFEST.in" "e:\ai智能体\gracy_framework\"
```

---

## 四、代码修改

### 4.1 修改 `core/__init__.py`

**删除**以下导入：
```python
# 删除 monitor_manager 相关
def _get_monitor_manager():
    from .monitor import monitor_manager
    return monitor_manager

# 删除 callback_base 导入
from .handler import callback_base
```

**保留**的核心：
- `plugin_manager`
- `security_manager`
- `config_manager`
- `logger_manager`
- `runtime_registry`

### 4.2 修改 `core/config.py`

**删除**：
```python
# 删除 AUTO_REPLIES 配置
config_manager.register_config(ConfigItem(
    key="auto_replies",
    default={...},  # 全部删除
    description="自动回复配置"
))

# 删除常量
AUTO_REPLIES = config_manager.get("auto_replies")

# 从 DEFAULT_CONFIG 删除 auto_replies 键
```

### 4.3 修改 `core/utils.py`

**删除**：
```python
# 删除自动回复相关
from core.config import AUTO_REPLIES  # 从导入中删除

def handle_auto_reply(msg: str) -> Optional[str]:
    # 全部删除
```

### 4.4 修改 `core/main.py`

**删除**以下导入：
```python
from core.handler import callback_base          # 删除
from core.monitor import monitor_manager, register_health_check_routes  # 删除
```

**删除**以下函数：
```python
def _interactive_connection_setup():
    # 整个函数删除（应用层交互菜单）
```

**修改** `callback()` 路由：
- 删除 `monitor_manager.record_message_received()` 调用
- 删除 `monitor_manager.record_message_error()` 调用
- 删除 `monitor_manager.record_message_processed()` 调用
- 删除 `callback_base()` 调用，改用 Pipeline 处理

**修改** `safe_shutdown()`：
- 删除 `monitor_manager.shutdown()` 调用

**修改** `run_bot()`：
- 删除 `_interactive_connection_setup()` 调用
- 删除 `register_health_check_routes(app)` 调用
- 删除 monitor_manager 相关日志

### 4.5 修改 `pyproject.toml`

```toml
[tool.setuptools.packages.find]
include = ["core*"]  # 只打包 core，不包含 plugins

[tool.setuptools.package-data]
core = ["gracy_adapter/**/*.json", "gracy_adapter/**/*.template.json"]

# 删除 CLI 入口（可选，如果要保留 CLI 则不删）
# [project.scripts]
# gracy = "core.tools.cli.main:app"
```

---

## 五、最终验证

完成上述步骤后，检查：

```powershell
# 查看最终目录结构
tree "e:\ai智能体\gracy_framework" /F
```

确认：
- [ ] `core/tools/` 不存在
- [ ] `core/gracy_session/` 不存在
- [ ] `core/monitor.py` 不存在
- [ ] `core/handler.py` 不存在
- [ ] `core/adapter.py` 不存在
- [ ] `core/gracy_adapter/` 完整存在（含 onebot、qq_official）
- [ ] `core/gracy_adapter/onebot/onebot_config.json` 不存在（只保留 template）
- [ ] 没有 `__pycache__` 目录
- [ ] 没有 `.bak` / `.backup` 文件

---

## 六、后续工作（可选）

1. **创建示例插件**：保留 `plugins/ExamplePlugin/` 作为模板
2. **更新文档**：修改文档中的路径引用
3. **添加 .gitignore**：排除 `config.json`、`logs/`、`__pycache__/`、`onebot_config.json`
4. **发布准备**：更新 `pyproject.toml` 的 metadata（名称、描述、作者）
5. **测试安装**：`pip install -e .` 验证导入是否正常
