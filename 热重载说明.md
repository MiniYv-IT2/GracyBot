# GracyBot 热重载计划书

> 废弃旧的 `_load_hotreload_config()` + `hotreload.json` 方案，重新设计。

---

## 一、现状

- `hotreload.json` 已丢失
- `_load_hotreload_config()` 已从 `core/main.py` 移除
- 现有 `/热重载` 命令只能写标记 + 重启进程，不是真正的热重载
- 框架启动用纯 `hypercorn.asyncio.serve()`，无 reload 参数
- `plugin_manager.reload_plugin()` 有定义但未实现真正的模块重载

---

## 二、业界参考

| 框架/方案 | 做法 | 可借鉴点 |
|-----------|------|---------|
| **NoneBot2 nb-cli** | 子进程 spawn + `watchfiles` 监控，`FileFilter` 精确过滤，信号安全终止 | 子进程模型成熟可靠 |
| **企业级插件系统** | SHA256 内容比对（非 mtime），沙箱隔离加载，灰度发布控制 | 避免误触发，安全 |
| **Trading Bot 零停机** | `control.json` 控制文件作信令，事件循环协程安全点触发重载 | 优雅停机点设计 |
| **importlib.reload** | 模块级就地重载，旧引用问题 | 适合轻量非 core 插件 |
| **Reloadium** | 深度 AST 依赖分析 + 对象热替换 | 太重，暂不采纳 |

**核心结论：** 子进程重启 = 可靠但重启所有；模块级 reload = 轻量但有坑。GracyBot 应该**两者结合**：
- 插件开发调试 → 模块级 reload（快、保留状态）
- 核心框架/适配器变更 → 子进程重启（干净、可靠）

---

## 三、目标架构

### 整体设计

```
gracy run (CLI)
  └─ core/main.py
       ├─ [启动阶段] 注册 Reloader
       │    ├─ 文件监控线程（watchfiles）
       │    └─ 信号处理（SIGINT/SIGTERM）
       │
       ├─ [运行阶段] 文件变化事件
       │    ├─ 命中 exclude 列表 → 忽略
       │    ├─ 命中 插件目录   → 模块级 reload 该插件
       │    ├─ 命中 core/      → 标记"需重启"，等待安全点
       │    └─ 命中 配置文件   → 热加载配置（不重启）
       │
       └─ [安全点] 消息处理间隔 / Pipeline 空闲
            ├─ 模块级 reload: importlib.reload → 重新注册 handler → 继续
            └─ 进程级重启: spawn 新进程 → 旧进程优雅退出
```

### 文件结构

```
gracybot/
  ├── hotreload/
  │   ├── __init__.py          # 导出 Reloader
  │   ├── watcher.py           # 文件监控（watchfiles 封装）
  │   ├── reloader.py          # 模块级 reload 逻辑
  │   ├── process.py           # 子进程 spawn/terminate
  │   └── filter.py            # FileFilter 模式过滤
  ├── hotreload.json           # 热重载开关（保留，由 /热重载 命令管理）
  └── status.json              # 运行状态文件（控制文件模式）
```

---

## 四、分步实施

### Step 1：文件监控基础设施

依赖 `watchfiles`（基于 Rust，跨平台，异步兼容）。

```python
# hotreload/filter.py
class FileFilter:
    includes: list[str]     # 默认: ["*.py", "*.toml", "*.json"]
    excludes: list[str]     # 默认: [".*", "__pycache__", "*.pyc", ".venv"]
    exclude_dirs: list[str] # 默认: [".git", "__pycache__", ".venv"]

    def accept(self, path: str) -> bool: ...
```

```python
# hotreload/watcher.py
class FileWatcher:
    """封装 watchfiles.awatch，异步迭代文件变化事件"""
    async def watch(self, dirs: list[Path], filter: FileFilter):
        async for changes in awatch(*dirs):
            yield [c for c in changes if filter.accept(c[1])]
```

### Step 2：模块级插件 reload

```python
# hotreload/reloader.py
class PluginReloader:
    """模块级插件重载，不涉及 core 框架"""

    def reload_plugin(self, plugin_name: str) -> bool:
        """完整流程：
        1. 从 sys.modules 删除该插件的所有子模块
        2. 调用 plugin_manager.reload_plugin() 重新加载
        3. 重新扫描 @on_command 装饰器
        4. 重新注册 handler
        """
        ...

    def safe_point(self) -> bool:
        """检查当前是否处于可安全 reload 的时机：
        - Pipeline 无正在处理的请求
        - 无进行中的 AI 生成
        - 适配器无未完成的上传
        """
        ...
```

### Step 3：核心变更进程级重启

```python
# hotreload/process.py
class ProcessReloader:
    """核心代码变更时，spawn 新进程替代旧进程"""

    async def restart(self):
        """1. 写 status.json 标记"重启中"
        2. spawn 新进程（gracy run）
        3. 等待新进程就绪（health check）
        4. 旧进程优雅退出（完成当前请求后）
        """
        ...
```

### Step 4：集成到启动流程

```python
# core/main.py 改动点

# 启动阶段
reloader = Reloader(
    watch_dirs=["plugins/", "core/", "style/"],
    filter=FileFilter(),
)

# 注册到事件循环
asyncio.create_task(reloader.watch_loop())

# 安全点注入
# Pipeline 每条消息处理完成后 → reloader.check_and_reload()

# 关闭阶段
await reloader.shutdown()
```

### Step 5：/热重载 命令重构

保留 Xiaoyu_Plugin 的 `/热重载` 命令，功能升级：

| 命令 | 功能 |
|------|------|
| `/热重载 状态` | 查看当前开关状态 + 变更文件列表 |
| `/热重载 开启` | 开启文件监控 |
| `/热重载 关闭` | 关闭文件监控 |
| `/热重载 重载 [插件名]` | 手动触发指定插件 reload |
| `/热重载 重启` | 完全重启进程（原行为） |

---

## 五、安全策略

| 问题 | 方案 |
|------|------|
| **插件 reload 后旧引用未更新** | 插件类推荐使用 `@with_logger` 等装饰器，避免 `from x import y` 裸引用 |
| **模块级副作用重复执行** | `initialize()` 应幂等，或加 `_initialized` 守卫 |
| **core 代码变更误 reload** | FileFilter 将 `core/` 排除出模块级 reload，仅触发进程级重启 |
| **频繁保存触发连续 reload** | debounce 1.5s，仅最后一次保存触发 |
| **reload 失败回滚** | 保留旧模块引用，reload 失败时恢复 |
| **热重载中状态丢失** | session / connection pool 等全局状态移至独立模块，reload 时不清理 |

---

## 六、优先级

| 优先级 | 内容 | 预估工作量 |
|--------|------|-----------|
| **P0** | FileFilter + FileWatcher 基础监控 | 1d |
| **P0** | 模块级插件 reload 实现 | 2d |
| **P1** | 集成到 core/main.py 启动流程 | 1d |
| **P1** | safe_point 安全点检测 | 1d |
| **P2** | 进程级重启（核心代码变更） | 2d |
| **P2** | /热重载 命令升级 | 0.5d |
| **P3** | status.json 控制文件 + 灰度控制 | 1d |

---

## 七、不做的范围

- ❌ 深度 AST 依赖分析（太重，Reloadium 模式）
- ❌ 核心模块（`core/`）模块级 reload（太危险，只走进程重启）
- ❌ C extension / 第三方库热重载（Python 不支持）
- ❌ Web UI 热重载控制面板（超出范围）
