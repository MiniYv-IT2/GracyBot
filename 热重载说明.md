# GracyBot 热重载说明

## 为什么启动会打印两遍？

加上 Hypercorn 热重载后，`python bot.py` 启动变成了**父子双进程**架构：

```
python bot.py
  └─ 父进程: import 所有模块 → 模块级代码执行一遍 → run_simple() 孵化子进程 → 只监听文件变化
       └─ 子进程: 重新 import 所有模块 → 模块级代码又执行一遍 → 真正跑 Quart 服务
```

两个进程走完全相同的 `import` 路径，所有写在模块顶层（不在函数/类内部）的代码都会各执行一次。

## 解决方案

所有模块级日志/初始化输出用 `WERKZEUG_RUN_MAIN` 环境变量守卫，只在子进程中执行：

```python
import os as _os
if _os.environ.get("WERKZEUG_RUN_MAIN") == "true":
    logger.info("这条日志只会在子进程打印一次")
```

父进程唯一需要输出的日志是 `🔄 热重载已启用`（在 `core/main.py` 的 `if not _is_reloader_worker` 分支中）。

## 已加守卫的文件

| 文件 | 抑制内容 |
|------|---------|
| `core/logger_manager.py` | Logo + "日志系统初始化完成" |
| `core/__init__.py` | "核心模块加载完成" |
| `core/handler.py` | "handler.py 加载完成" |
| `core/monitor.py` | "监控管理器已初始化" |

## 插件文件热重载

Hypercorn 的 StatReloader 默认只监听父进程 `sys.modules` 中已 import 的文件。插件是子进程动态加载的，父进程不知道它们存在，所以改了也不会触发重载。

修复：在 `core/main.py` 的 `run_simple()` 调用中传入 `extra_files` 参数，显式 glob 所有 `plugins/**/*.py` 加入监听。

## 插件开发注意事项

handler.py 调用插件时不读取返回值，插件必须通过 `gracy_send_msg`（第二个参数）主动发送消息，不能 `return` 字符串。
